import { writable } from "svelte/store";

export const voyage = writable(null);
export const excursion = writable(null);

export const selected = writable(null);
export const undo = writable(null);
export const announce = writable(null);

// ok dude remove this at some point no way this is right
export const currentShipType = writable(null);


  
  
